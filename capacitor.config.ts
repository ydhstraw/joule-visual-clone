import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '36572_yudhistira_tugas1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
